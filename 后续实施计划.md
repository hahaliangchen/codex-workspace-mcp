# Rust Agent 与 DSH 前端接入后续计划

## 目标

让本项目的 Rust Agent 独立运行任务，并复用 DeepSeek Harness（DSH）的聊天页面和步骤轨迹视图。用户从页面提交任务后，可以实时查看模型与工具步骤；任务结束或页面刷新后，仍能读取完整历史。现有本地工具、工作记忆和代码索引继续复用。

## 当前进度

- 本项目已提供独立 Agent 任务接口、顺序模型与工具循环、任务取消、SQLite 事件历史、SSE 订阅和启动时中断任务恢复。
- 会话事件采用 DSH SessionEvent 的基本字段和聊天、工具、步骤事件，便于后续接入页面。
- DSH 前端接入和端到端页面验证尚未完成；相关前端位于 `/home/goodtime/project/deepseek-harness/packages/client`。
- 现有 Codex `/v1/responses` 代理入口仍保留，主入口切换和旧配置清理尚未完成。

## 后续阶段

### 1. 用固定事件验证前端

- 在 DSH `packages/client` 中复用 `ui-chat` 和 `ui-trajectory`，准备覆盖用户消息、模型步骤、工具调用及结果、步骤耗时的固定事件记录。
- 验证聊天和轨迹组件实际读取的事件字段、事件顺序与历史加载方式，并据此确认 Rust API 与前端适配层的数据契约。
- 验收：固定事件可完整显示为聊天内容和可展开步骤，刷新后仍可从历史记录重建。

### 2. 完成会话事件契约

- 对齐 `turn/start`、`user/message`、`step/start`、`assistant/message`、`tool/call`、`tool/result`、`step/end` 和 `turn/end` 的载荷与终止原因。
- 明确序号、时间、取消、失败、重连补发和服务重启恢复语义；区分一次 Agent 会话与跨任务工作记忆。
- 验收：同一任务的持久事件可以按序重放，进行中任务在服务重启后有明确的中断状态。

### 3. 验证并补齐 Rust 任务循环

- 使用假模型验证多轮工具调用、工具错误、模型错误、步数上限、取消和崩溃恢复。
- 检查每次模型步骤与工具结果是否都形成可恢复事件，检查错误能否传回模型或结束任务。
- 验收：不经过 Codex 代理也能完成多步任务，异常和取消都能留下可解释的最终状态。

### 4. 完成 DSH 页面适配

- 在 DSH 前端增加连接本项目 Rust 服务的适配层，覆盖任务提交、会话列表、事件订阅、历史读取和取消。
- 将实时及恢复后的任务事件接入 `ui-chat` 与 `ui-trajectory`，展示模型步骤、工具名称、结果和耗时。
- 验收：用户可从 DSH 页面提交任务、查看实时步骤、取消任务、断线后续接并打开历史任务。

### 5. 端到端验证与入口迁移

- 从 DSH 页面跑通任务提交、文件与索引工具调用、受限命令执行、最终回答及历史恢复。
- 记录配置和服务启动方式，验证 Rust Agent 在没有 Codex 服务参与时仍可完成任务。
- 集成验收通过后，再决定如何将 Rust Agent 设为主要入口，并清理不再使用的 Codex 代理路径、子 Agent 声明和相关配置。

## 首版边界

首版维持单 Agent、单模型提供方、顺序工具调用、持久会话、取消能力、聊天页面和步骤轨迹。模型文字流式输出、并行工具、子 Agent、插件系统及 DSH 其他页面作为后续独立阶段，不作为前端接通的前置条件。

## 执行顺序

先完成固定事件前端验证，再确认事件契约和后端边界，然后实现页面适配并进行端到端验收。只有在独立运行和历史恢复均通过后，才切换主入口并清理旧路径。
